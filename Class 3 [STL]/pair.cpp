#include <bits/stdc++.h>
using namespace std;
int main()
{
//    pair<int, int> point; //point.first, point.second;
//    cin >> point.first >> point.second;
//    cout << point.first << " " << point.second << '\n';

//    pair<pair<int, int>, int> point;
//    cin >> point.first.first >> point.first.second >> point.second;
//    cout << point.first.first << " " << point.first.second << " " << point.second << '\n';

//    pair<int, int> arr[5];
//    for(int i = 0; i < 3; i++)
//        cin >> arr[i].first >> arr[i].second;
//
//    for(int i = 0; i < 3; i++)
//        cout << arr[i].first << " " << arr[i].second << '\n';
//    cout << '\n';

    pair<pair<pair<int, int>, int>, int> point;
    point.first.first.first
    point.first.second
}
